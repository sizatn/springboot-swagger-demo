<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sizatn.ssd.dao.primary.UserMapper">
	<resultMap id="BaseResultMap" type="com.sizatn.ssd.entity.User">
		<id property="id" column="ID" jdbcType="INTEGER" />
        <result property="userNo" column="user_no" jdbcType="VARCHAR" />
        <result property="userName" column="user_name" jdbcType="VARCHAR" />
        <result property="password" column="password" jdbcType="VARCHAR" />
        <result property="gender" column="gender" jdbcType="VARCHAR" />
        <result property="phone" column="phone" jdbcType="VARCHAR" />
        <result property="mobile" column="mobile" jdbcType="VARCHAR" />
        <result property="enabled" column="enabled" jdbcType="VARCHAR" />
        <result property="createDate" column="create_date" jdbcType="TIMESTAMP" />
        <result property="updateDate" column="update_date" jdbcType="TIMESTAMP" />
	</resultMap>

	<select id="getUserList" resultMap="BaseResultMap" >
		SELECT * FROM user
	</select>
	
	<insert id="saveUser" parameterType="User">
		INSERT INTO user (user_no, user_name, password, gender, phone, mobile, enabled, create_date, update_date) VALUES 
		(#{userNo}, #{userName}, #{password}, #{gender}, #{phone}, #{mobile}, #{enabled}, NOW(), NOW())
	</insert>
	
	<select id="getUser" resultMap="BaseResultMap" parameterType="String">
		SELECT * FROM user WHERE user_no = #{userNo}
	</select>
	
	<update id="updateUser" parameterType="User">
        UPDATE user SET user_name = #{userName}, password = #{password}, gender = #{gender},
        phone = #{phone}, mobile = #{mobile}, enabled = #{enabled}, update_date = NOW()  WHERE user_no = #{userNo}
	</update>
	
	<delete id="deleteUser" parameterType="String">
		DELETE FROM user where user_no = #{userNo}
	</delete>
	
</mapper>